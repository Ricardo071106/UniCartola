# ═══════════════════════════════════════════════════════════════
# CAMPUS LEAGUE — Render ou .env.local
# ═══════════════════════════════════════════════════════════════

# ── APP ───────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://seu-app.onrender.com

# ── SESSÃO (opcional, para quando o auth voltar) ──────────────
# SESSION_SECRET=[openssl rand -base64 48]

# ── BANCO (removido — em redesign) ────────────────────────────
# Quando repensar o schema, recrie o banco no Supabase e use:
# DATABASE_URL=postgresql://postgres:[SENHA]@db.[REF].supabase.co:5432/postgres
# SUPABASE_REGION=us-west-2
#
# Para zerar o banco antigo:
# DATABASE_URL="..." npx tsx scripts/drop-database.ts
